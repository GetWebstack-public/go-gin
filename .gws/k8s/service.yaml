apiVersion: v1
kind: Service
metadata:
  name: go-gin
  namespace: go-gin
  labels:
    app: go-gin
spec:
  type: ClusterIP
  selector:
    app: go-gin
  ports:
  - port: 50051
    targetPort: 50051
    protocol: TCP
    name: grpc
    appProtocol: kubernetes.io/h2c
